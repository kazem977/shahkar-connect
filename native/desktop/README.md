# Desktop sing-box

`DesktopVpnEngine` starts `SINGBOX_BIN` or `native/bin/sing-box` with
`sing-box run -c <json>`. Fetch a release with `./tool/fetch_singbox.sh`.
TUN on Linux needs CAP_NET_ADMIN; Windows/macOS follow sing-box docs.
